
  




/* Basic container styles */
.app-container {
    display: flex;
    flex-direction: column;
    transition: margin-left 0.3s ease-in-out; /* Smooth transition for content shift */
  }
  
  /* Styling the hamburger icon */
  .hamburger-icon {
    position: fixed;
    top: 20px;
    left: 20px;
    cursor: pointer;
    z-index: 1100; /* Make sure the hamburger icon is above other content */
  }
  
  .content {
    margin-left: 550px; /* Initially push the content to the right to accommodate the sidebar */
    transition: margin-left 0.3s ease; /* Smooth transition for content when sidebar opens/closes */
  }
  
  .sidebar.open + .content {
    margin-right: 0; /* When sidebar is open, push the content back */
  }
  .sidebar {
    position: fixed;
    left: -250px;
    top: 0;
    width: 250px;
    height: 100%;
    background-color: #556d8a;
    color: white;
    transition: left 0.3s;
    padding: 20px;
    overflow-y: auto;
  }
  
  /* .sidebar.open {
    left: 0;
  } */
  
  .sidebar ul {
    list-style: none;
    padding: 0;
  }
  
  .sidebar ul li {
    margin: 15px 0;
  }
  
  .sidebar ul li a {
    color: white;
    text-decoration: none;
    font-size: 18px;
  }
  
  .sidebar ul li a:hover {
    text-decoration: none;
  }
  
  .hamburger-icon {
    position: fixed;
    top: 20px;
    left: 20px;
    cursor: pointer;
    z-index: 1000;
  }
  
  .content {
    margin-left: 250px; /* Matches the sidebar width */
    padding: 20px;
    flex-grow: 1;
    transition: margin-left 0.3s ease-in-out;
    transition: margin-left 0.3s;
  }
  .app-container.sidebar-open .content {
    margin-left: 250px; /* Adjust content when sidebar is open */
  }
  /* .app-container .content {
    margin-left: 0;
  } */
  
  